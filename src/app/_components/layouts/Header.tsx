import React from 'react';
import Navigation from './Navigation';

const Header: React.FC = () => {
    return(
        <>
        <header></header>
        <Navigation menuId={2} />
        </>
    );
}

export default Header;